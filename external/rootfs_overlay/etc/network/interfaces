auto lo
iface lo inet loopback

#auto eth0
#iface eth0 inet dhcp
#  hostname $(hostname)

#auto usb0
#iface usb0 inet dhcp
#  hostname $(hostname)

auto wlan0
iface wlan0 inet static
  address 10.0.0.1
  netmask 255.255.255.0
  pre-up /usr/bin/wifi-init.sh
  post-down /usr/bin/wifi-stop.sh
